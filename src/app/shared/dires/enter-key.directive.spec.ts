import { EnterKeyDirective } from './enter-key.directive';

describe('EnterKeyDirective', () => {
  it('should create an instance', () => {
    const directive = new EnterKeyDirective();
    expect(directive).toBeTruthy();
  });
});
