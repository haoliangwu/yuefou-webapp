import { RecipeFilePrefixPipe } from './recipe-file-prefix.pipe';

describe('RecipeFilePrefixPipe', () => {
  it('create an instance', () => {
    const pipe = new RecipeFilePrefixPipe();
    expect(pipe).toBeTruthy();
  });
});
