import { AvaliableOperationPipe } from './avaliable-operation.pipe';

describe('AvaliableOperationPipe', () => {
  it('create an instance', () => {
    const pipe = new AvaliableOperationPipe();
    expect(pipe).toBeTruthy();
  });
});
